// for

for (inicializador; condição; consideração_final)
  for (let i = 1; i <= 10; i++) {
    console.log(i);
  }

// array e for
const arr = [1, 2, 3]; // lista (array)
for (let i = 0; i <= arr.length; i++) {
  console.log(arr, arr.length);
}
