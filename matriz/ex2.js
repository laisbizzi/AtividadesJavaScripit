//Insira no fim uma nova linha com 3 colunas. Exiba no console a matriz.
//Insira no início uma nova linha com 3 colunas. Exiba no console a matriz.
//Remova do fim uma linha. Exiba no console a linha removida.
//Remova do início uma linha. Exiba no console a linha removida
//Exiba no console a matriz.

const numbers = [
  [10, 20, 60],
  [8, 10, 52],
];

numbers.push([2, 4, 6]);
console.log(numbers);
numbers.unshift([3, 5, 9]);
console.log(numbers);
numbers.pop[3];
console.log(numbers.pop(numbers[3]));
numbers.shift[0];
console.log(numbers.pop(numbers[0]));
console.log("Sua matriz final é: ", numbers);
